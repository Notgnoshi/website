[uwsgi]

# Django-related settings:
# Base directory
chdir = BASE_REPOSITORY_PATH/mc.agill.xyz/
# Django's wsgi file
module = mc_agill_xyz.wsgi

# Process-related settings:
master = true
processes = 10
socket = /tmp/mc.agill.xyz.sock
uid = www-data
gid = www-data

# Clear environment on exit
vacuum = true
